@using Application.Localization
<div ng-controller="app.manager.areaAgent.areaAgent.index as vm">
    <div class="view-page-head margin-bottom-5">
        <h1 class="view-page-title">
            <span>@L("AreaAgents")</span> <small>@L("AreaAgentsHeaderInfo")</small>
        </h1>
        <a class="button-text primary" ui-sref="areaAgent.areaAgent.createOrEdit()"><i class="icon-plus"></i> @L("CreateNewAreaAgent")</a>
    </div>
    <div class="portlet light">
        <div class="responsive-table-container">
            <table class="table table-bordered table-striped table-hover" id="areaAgentTable">
                <thead>
                    <tr>
                        <th>@L("Name")</th>
                        <th>@L("CreationTime")</th>
                        <th>@L("Actions")</th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="areaAgent in vm.areaAgents">
                        <td>{{areaAgent.name}}</td>
                        <td>{{areaAgent.creationTime|momentFormat: 'YYYY-MM-DD HH:mm:ss'}}</td>
                        <td>
                            <div class="dropdown-wripper dropdown-menu">
                                <a data-toggle="dropdown" class="button-text">@L("Actions")<i class="icon icon-angle-down dropdown-sign"></i></a>
                                <ul class="dropdown dropdown-menu-list left hide" data-ride="dropdown">
                                    <li><a ui-sref="areaAgent.areaAgent.createOrEdit({id:areaAgent.id})">@L("Edit")</a></li>
                                    <li><a ui-sref="areaAgent.areaAgent.areaAgencyAreas({id:areaAgent.id})">@L("AreaAgencyAreas")</a></li>
                                    <li><a ng-click="vm.delete(areaAgent)">@L("Delete")</a></li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
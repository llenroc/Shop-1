<!DOCTYPE html>
<html lang="en">
@Html.Partial("_Head")
<body ng-app="app" class="gray-body height-full">
    <div class="page" ng-controller="app.mobile.layout as vm">
        @Html.Action("Header", "Layout", new { PageTitle = ViewBag.PageTitle })
        <div class="page-content">
            @RenderBody()
        </div>
    </div>
</body>
</html>
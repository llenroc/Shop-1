<!DOCTYPE html>
<html lang="en">
@Html.Partial("_Head")
<body ng-app="app" class="gray-body height-full">
    <div class="page" ng-controller="app.mobile.layout as m">
        @Html.Action("Header", "Layout", new { PageTitle = ViewBag.PageTitle })
        @Html.Action("Nav", "Layout")
        <div class="page-content">
            @RenderBody()
        </div>
        @Html.Action("Footer", "Layout")
    </div>
</body>
</html>
